---
- name: Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: Stop apache service
      ansible.builtin.service:
        name: httpd
        state: stopped
        enabled: false

    - name: Uninstall Apache
      ansible.builtin.dnf:
        name: httpd
        state: absent
